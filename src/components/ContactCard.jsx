{/* ContactCard.jsx */}

import '../../styles/contactcard.css';

export default function ContactCard({
    title,
    description,
    link,
    linkDescription
    }) {
    return (
        
    );
}